locations:
  - location_name: main-production-process
    code_source:
      package_name: process1.definitions
    build:
      directory: ./
      #registry: <account-id>.dkr.ecr.us-west-2.amazonaws.com/<image-name>
      # registry: ghcr.io/<org>/<image-name>
      registry: rjacdevops/cluster-base:latest
